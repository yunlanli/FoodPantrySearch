import { StyleSheet } from 'react-native';

export const userMap = StyleSheet.create({

    pantryDescription: {
        display: 'flex',
        flexDirection: 'column',
    },

    mapContainer: {
        width: '100%',
        height: '70%',
    },

})